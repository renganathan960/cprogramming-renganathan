#include <stdio.h>

int main() {
    int n1, n2;
    
    scanf("%d", &n1);
    int a[n1];
    for(int i = 0; i < n1; i++) {
        scanf("%d", &a[i]);
    }
    
    scanf("%d", &n2);
    int b[n2];
    for(int i = 0; i < n2; i++) {
        scanf("%d", &b[i]);
    }
    
    int i = 0, j = 0;
    int last = -1;   // to track last printed element
    
    while(i < n1 && j < n2) {
        int current;
        
        if(a[i] < b[j]) {
            current = a[i++];
        } 
