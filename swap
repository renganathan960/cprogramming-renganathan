#include <stdio.h>

void swap(int a, int b) {
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    printf("Before Swap: %d %d\n", a, b);

    swap(a, b);   // pass by value

    printf("After Swap: %d %d", b, a);

    return 0;
}
